services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ledgerline
      POSTGRES_USER: ledgerline
      POSTGRES_PASSWORD: local-root-only
    ports:
      - "5433:5432"
    volumes:
      - ledgerline_pg:/var/lib/postgresql/data
volumes:
  ledgerline_pg:
